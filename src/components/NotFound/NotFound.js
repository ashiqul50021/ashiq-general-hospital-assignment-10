import React from 'react';
import error from '../../images/notfound.png';

const NotFound = () => {
    return (
        <div>
            <img src={error} alt="" />
        </div>
    );
};

export default NotFound;